import { MediaType } from '../../constants/mediaConstants';
import MediaSearch from './components/MediaSearch';

const MovieSearch = () => <MediaSearch mediaType={MediaType.MOVIE} />;

export default MovieSearch;
